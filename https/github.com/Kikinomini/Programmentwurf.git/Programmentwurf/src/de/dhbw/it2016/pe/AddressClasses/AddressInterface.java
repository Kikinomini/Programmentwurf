package de.dhbw.it2016.pe.AddressClasses;

public interface AddressInterface {
	
	// Implements a method so as to adjust the address to the corresponding
	// country specifications.
	public String formatAddress();
}
